__pycache__/
*.py[cod]
.venv/
.env
.pytest_cache/
dist/
build/
*.egg-info/
.DS_Store

# Audit logs are regenerated on every run and are noisy.
results/audit/
results/*.jsonl

# Real-model output stays out of the repository: it is a sample from a
# stochastic process and one local run should not become canonical
# evidence. The deterministic results in results/ are committed instead.
results/real_model_*.json
*.bundle
